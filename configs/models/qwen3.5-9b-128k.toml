# Local Qwen 3.5 9B (mid), Q4_K_M, 128k context (Modelfile re-tag).
# Size-axis: mid qwen3.5 — fits in 16 GB VRAM/UMA budget
# (6.6 GB model + KV cache for 128k context).
#
# prefill_no_think required for qwen3.5 family.
name = "qwen3.5-9b-128k"
suppress_thinking = true

[client]
base_url = "http://localhost:11434"
model = "qwen3.5:9b-128k"
api_key = "not-needed"
temperature = 0.0
max_tokens = 1500
timeout = 1800
prefill_no_think = true
